function render(){const d=load(),q=document.getElementById('search').value.toLowerCase(),cat=document.getElementById('category').value,type=document.getElementById('type').value,status=document.getElementById('status').value;let a=d.listings.filter(l=>(!q||(l.itemName+' '+l.description+' '+l.category).toLowerCase().includes(q))&&(!cat||l.category===cat)&&(!type||l.listingType===type)&&(!status||l.status===status));document.getElementById('listingGrid').innerHTML=a.length?a.map(card).join(''):`<div class="empty"><b>No listings found</b>Try changing your filters or search.</div>`}document.addEventListener('DOMContentLoaded',()=>{const p=new URLSearchParams(location.search);if(p.get('type'))document.getElementById('type').value=p.get('type');['search','category','type','status'].forEach(x=>document.getElementById(x).addEventListener('input',render));render()})