document.addEventListener('DOMContentLoaded',()=>document.getElementById('requestForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target),d=load(),r={id:Date.now(),title:f.get('title'),description:f.get('description'),category:f.get('category'),department:f.get('department'),year:+f.get('year'),requesterId:currentUser().id,status:'OPEN',createdAt:new Date().toISOString().slice(0,10)};d.requests.unshift(r);save(d);toast('Request posted ✦');setTimeout(()=>location.href='my-requests.html',500)})